spring.application.name=TruckMitra

# ==========================
# 1. Server Configuration
# ==========================
server.port=${PORT:8080}
spring.devtools.restart.enabled=false

# ==========================
# 2. PostgreSQL Database Configuration
# ==========================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-spring-poetry-aira0grk-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_PZGd8EphIbf9}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Physical Naming Strategy
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ==========================
# 3. JWT Configuration
# ==========================
application.security.jwt.secret-key=${JWT_SECRET_KEY:dummy_secret_key_that_is_long_enough_for_hs256}
# 30 Minutes (Access Token)
application.security.jwt.expiration=1800000
# 30 Days (Refresh Token)
application.security.jwt.refresh-token.expiration=2592000000

# ==========================
# 4. Twilio Configuration
# ==========================
twilio.account.sid=${TWILIO_ACCOUNT_SID:dummy}
twilio.auth.token=${TWILIO_AUTH_TOKEN:dummy}
twilio.verify.service.sid=${TWILIO_VERIFY_SERVICE_SID:dummy}
twilio.phone.number=${TWILIO_PHONE_NUMBER:dummy}

# ==========================
# 5. OAuth2 Configuration
# ==========================
# Google OAuth2 Settings
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:dummy}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:dummy}
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google

# Facebook OAuth2 Settings
spring.security.oauth2.client.registration.facebook.client-id=${FACEBOOK_CLIENT_ID:dummy}
spring.security.oauth2.client.registration.facebook.client-secret=${FACEBOOK_CLIENT_SECRET:dummy}
spring.security.oauth2.client.registration.facebook.redirect-uri={baseUrl}/login/oauth2/code/facebook
spring.security.oauth2.client.registration.facebook.scope=email,public_profile

# ==========================
# 6. Email (SMTP) Configuration
# ==========================
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:dummy_username}
spring.mail.password=${SMTP_PASSWORD:dummy_password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================
# 7. File Upload & Cloudinary Config
# ==========================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=./uploads
file.base-url=${FILE_BASE_URL:http://localhost:8080}
cloudinary.cloud-name=dvzfhvlpa
cloudinary.api-key=175311112735636
cloudinary.api-secret=YOUR_API_SECRET_HERE
# ==========================
# 8. Razorpay
# ==========================
razorpay.key.id=${RAZORPAY_KEY_ID:dummy}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:dummy}

# ==========================
# 9. Async Thread Pool
# ==========================
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

# ==========================
# 10. Logging
# ==========================
logging.level.org.springframework.security=TRACE
logging.level.com.truckmitra=INFO

# ==========================
# 11. Socket relay server
# ==========================
socket.server.url=${SOCKET_SERVER_URL:http://localhost:3000}

# ==========================
# 12. Flyway configuration
# ==========================
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false
spring.flyway.repair-on-migrate=true
spring.flyway.locations=classpath:db/migration

# ==========================
# 13. Swagger / OpenAPI
# ==========================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# ==========================
# 14. Business Logic Configurations
# ==========================
truckmitra.diesel.rate=95.0
